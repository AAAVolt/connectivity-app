"""Tenant middleware (reserved for future use).

For the MVP, tenant resolution is handled via the get_tenant FastAPI
dependency in deps.py. This module is reserved for future middleware
that could resolve tenant from subdomain or path prefix.
"""
